#!/usr/bin#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
##!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3.#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4.#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")"#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONT#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${B#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1";#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        l#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback:#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
    for cmd in python3.11 python3#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
    for cmd in python3.11 python3.12 python3.13 python3 python;#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
    for cmd in python3.11 python3.12 python3.13 python3 python; do
        if check_command "$cmd"; then#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
    for cmd in python3.11 python3.12 python3.13 python3 python; do
        if check_command "$cmd"; then
            local ver
            ver=$($cmd --version 2>&1 | grep -oE '[0-9]+\.[0-9]+' | head -1)
#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
    for cmd in python3.11 python3.12 python3.13 python3 python; do
        if check_command "$cmd"; then
            local ver
            ver=$($cmd --version 2>&1 | grep -oE '[0-9]+\.[0-9]+' | head -1)
            if awk "BEGIN {exit !($ver >=#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
    for cmd in python3.11 python3.12 python3.13 python3 python; do
        if check_command "$cmd"; then
            local ver
            ver=$($cmd --version 2>&1 | grep -oE '[0-9]+\.[0-9]+' | head -1)
            if awk "BEGIN {exit !($ver >= 3.11)}"; then
                echo#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
    for cmd in python3.11 python3.12 python3.13 python3 python; do
        if check_command "$cmd"; then
            local ver
            ver=$($cmd --version 2>&1 | grep -oE '[0-9]+\.[0-9]+' | head -1)
            if awk "BEGIN {exit !($ver >= 3.11)}"; then
                echo "$cmd"
                return 0
            fi
        fi
    done
    return#!/usr/bin/env bash
set -euo pipefail

# ============================================================
# 🎯 外贸获客 Agent - 启动脚本 (第一性原理设计)
# ============================================================
# 第一性原理：
# 1. 运行需要什么？ → Python 3.11+、pip、依赖包
# 2. 服务如何启动？ → python3 web_backend.py
# 3. 如何访问？     → 浏览器打开 http://localhost:8000
# 4. 出错了怎么办？ → 检查错误日志、端口占用、依赖缺失
# ============================================================

# 颜色定义
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m' # No Color

# 配置
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND_FILE="${SCRIPT_DIR}/web_backend.py"
FRONTEND_FILE="${SCRIPT_DIR}/web_frontend.html"
HOST="0.0.0.0"
PORT="8000"
API_KEY="sk-1247a1fea10b4f4db70d83dbc64edc97"
PYTHON_CMD=""
PID_FILE="/tmp/waimao-agent.pid"

# ----------------------------------------------------------
# 工具函数
# ----------------------------------------------------------
log_info()  { echo -e "${BLUE}[INFO]${NC}  $1"; }
log_ok()    { echo -e "${GREEN}[OK]${NC}   $1"; }
log_warn()  { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $1"; }
log_step()  { echo -e "${CYAN}${BOLD}[STEP]${NC} $1"; }

check_command() {
    command -v "$1" >/dev/null 2>&1
}

check_port() {
    local port="$1"
    if command -v lsof >/dev/null 2>&1; then
        lsof -i :"$port" >/dev/null 2>&1
    elif command -v netstat >/dev/null 2>&1; then
        netstat -tuln 2>/dev/null | grep -q ":$port "
    elif command -v ss >/dev/null 2>&1; then
        ss -tuln 2>/dev/null | grep -q ":$port "
    else
        # fallback: 尝试连接
        (echo >/dev/tcp/localhost/$port) 2>/dev/null
    fi
}

find_python() {
    # 优先找 python3.11+，其次 python3，最后 python
    for cmd in python3.11 python3.12 python3.13 python3 python; do
        if check_command "$cmd"; then
            local ver
            ver=$($cmd --version 2>&1 | grep -oE '[0-9]+\.[0-9]+' | head -1)
            if awk "BEGIN {exit !($ver >= 3.11)}"; then
                echo "$cmd"
                return 0
            fi
        fi
    done
    return 1
}

print_banner() {
    echo -e ""
    echo